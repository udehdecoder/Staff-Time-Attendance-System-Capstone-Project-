const express = require("express")

const router = express.Router()

// const {getAllAttendance, getUserAttendance} = require("../controllers/attendanceController")

// router.post("/getallattendance", getAllAttendance)
// router.post("/getuserattendance", getUserAttendance)

module.exports = router